# دليل تفعيل الإشعارات الحقيقية (Push Notifications)

هذا الدليل يشرح خطوة بخطوة إزاي تفعّل إشعارات حقيقية توصل على الموبايل
**حتى لو التطبيق مقفول تمامًا**، بتفكّرك بمواعيد تسليم الطلبات القريبة.

الفكرة بتعتمد على **نفس مشروع Firebase** اللي بتستخدمه أصلاً في المزامنة
السحابية بين الأجهزة. يعني لو المزامنة السحابية شغالة عندك بالفعل، إنت
فعلاً في نص الطريق.

---

## الملفات المرفقة وأماكنها

| الملف | مكانه |
|---|---|
| `index.html` | يستبدل الملف القديم — فيه إعدادات الإشعارات الجديدة |
| `firebase-messaging-sw.js` | يترفع **بجانب** `index.html` بالظبط على نفس السيرفر/الاستضافة |
| `functions/` (مجلد كامل) + `firebase.json` | بيترفعوا على مشروع Firebase (مش على استضافة الموقع) |

---

## الخطوة 1: تفعيل خطة الفوترة (Blaze)

الإشعارات بتحتاج "Cloud Function" تشتغل مرة كل يوم تلقائيًا، وده محتاج
خطة **Blaze** (ادفع حسب الاستخدام) في مشروع Firebase بتاعك.

1. افتح **console.firebase.google.com** → مشروعك.
2. من تحت (⚙️ Usage and billing) فعّل خطة **Blaze**.
3. الاستخدام هنا صغير جدًا (مرة واحدة يوميًا) — عمليًا هيفضل ضمن الحد
   المجاني الشهري من جوجل، ومش متوقع أي فاتورة فعلية.

## الخطوة 2: إنشاء مفتاح Web Push (VAPID key)

1. من نفس المشروع في Firebase Console: **⚙️ Project settings** →
   تبويب **Cloud Messaging**.
2. انزل لقسم **Web configuration** → **Web Push certificates**.
3. اضغط **Generate key pair**.
4. انسخ المفتاح الطويل اللي بيظهر (يبدأ عادة بحروف وأرقام طويلة).

## الخطوة 3: رفع الملفات على استضافة الموقع

- استبدل `index.html` القديم بالنسخة الجديدة.
- ارفع `firebase-messaging-sw.js` بجانبه في نفس المجلد بالظبط.

## الخطوة 4: تفعيل الإشعارات من داخل التطبيق

1. افتح التطبيق على الموبايل (لازم يكون مثبّت كـ PWA على الشاشة الرئيسية
   أو مفتوح من المتصفح، والمزامنة السحابية مفعّلة بالفعل).
2. روح لـ **الإعدادات** → كارت **🔔 إشعارات حقيقية لمواعيد التسليم**.
3. الصق مفتاح **VAPID** اللي نسخته في الخطوة 2.
4. اضغط **تفعيل الإشعارات على هذا الجهاز** ووافق على إذن الإشعارات.
5. كرر نفس الخطوة على أي جهاز تاني (موبايل صاحب الورشة، تابلت، إلخ) عايز
   يوصله نفس التنبيه.
6. اختياري: غيّر "نبّهني قبل التسليم بعدد الأيام" حسب رغبتك (افتراضي: يوم واحد).

## الخطوة 5: نشر Cloud Function (مرة واحدة فقط)

ده الجزء اللي بيبعت الإشعار فعليًا كل يوم. محتاج جهاز كمبيوتر عليه
Node.js مثبت (مرة واحدة بس، مش هتحتاجها بعد كده):

```bash
npm install -g firebase-tools
firebase login
cd المجلد-اللي-فيه-firebase.json-و-functions
firebase deploy --only functions
```

بعد ما ينجح الرفع، الدالة هتشتغل تلقائيًا كل يوم الساعة 8 الصبح (بتوقيت
القاهرة) وتبعت إشعار لأي طلب هيتسلم بعد عدد الأيام اللي حددته، لأي جهاز
مسجل عند أي ورشة.

> ملحوظة: لو مش عندك حد يعرف يشغّل الأوامر دي، ممكن تستخدم
> **Google Cloud Shell** (من داخل console.cloud.google.com، فيه Terminal
> جاهز في المتصفح من غير ما تثبت حاجة على جهازك) وتنفذ نفس الأوامر فيه
> بعد ما ترفع مجلد المشروع.

---

## أسئلة شائعة

**هل الإشعار بيوصل حتى لو قفلت التطبيق تمامًا؟**
أيوه، طالما الموبايل متصل بالإنترنت وعندك على الأقل جهاز واحد فعّلت
عليه الإشعارات من الإعدادات.

**فيه أكتر من طلب هيتسلم في نفس اليوم؟**
هيوصلك إشعار واحد مجمّع بعدد الطلبات، بدل ما توصلك رسائل كتير منفصلة.

**عايز أغيّر ميعاد الإشعار اليومي (مش الساعة 8 الصبح)؟**
غيّر السطر `schedule: '0 8 * * *'` في `functions/index.js` (فورمات
cron قياسي)، وبعدين اعمل `firebase deploy --only functions` تاني.
